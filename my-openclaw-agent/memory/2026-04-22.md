- WhatsApp outbound policy preference: use manual toggle only.
- Commands agreed with Geraldo:
  - "mode auto-reply on" => allow outbound replies/messages to other WhatsApp numbers.
  - "mode auto-reply off" => block outbound replies/messages to other WhatsApp numbers.
- Initial state file created at `state/auto-reply.json` with enabled=false.
- Added a hard instruction in `AGENTS.md`: when `state/auto-reply.json` has `enabled: false`, do not send outbound WhatsApp messages to other numbers unless Geraldo explicitly turns it on.
- Google Sheets rule for the existing expense tracker (`1_6Sb9TiMlAhT3Fi7BxTPSfmvvCo95Fegae4pP4SbHJ8`): `Sheet1` is for pengeluaran only, `Sheet2` is for pemasukan only.
- In both sheets, keep the `TOTAL` row as the final row of the table. New rows must always be inserted above `TOTAL`, not appended below it.
